# Tauri Wiring (Example)

This is *not* a complete Tauri project; it's a minimal example of the commands you'd expose.

Add these commands to your existing `src-tauri/src/main.rs` (or command module).
